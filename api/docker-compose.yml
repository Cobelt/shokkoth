version: '3.7'
services:
  api:
    build:
      context: .
    ports:
      - "5013:5013"
    environment:
      - MONGO_SERVER=mongodb
      - MONGO_PORT=27017
    networks:
      - shokkoth-universe

networks:
  shokkoth-universe:
    external:
        name: shokkoth-universe
